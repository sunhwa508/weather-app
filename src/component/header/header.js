import React from "react";
import { HeaderDetail } from "./header.styled";

const Header = () => {
  return (
    <HeaderDetail>
      <h2>HEADER</h2>
    </HeaderDetail>
  );
};

export default Header;
